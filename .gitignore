# ── Individual subject data files (large volume; regenerable from source) ──────
# Copies of Data_resources/COMPETITION_data_restricted/simple_format_data/
my_code/EDA_set/
my_code/Training_set/
my_code/Test_set/
my_code/Schedule0_set/
# Archived first-investigation folder keeps its own raw copies
my_code/previous_errors/initial_investigation/schedule_4/
my_code/previous_errors/initial_investigation/schedule_5/
my_code/previous_errors/initial_investigation/schedule_7/

# ── Generated output data files (CSV; regenerable by running the wrapper) ──────
my_code/catie_results/*.csv

# ── catie_calibration derived data (regenerable: sanitize_splits.py, build_cache) ─
my_code/catie_calibration/data/
my_code/catie_calibration/cache/
my_code/catie_calibration/**/*.csv.gz
# matlab/results/*.csv are small (<400KB) and kept tracked as evidence, EXCEPT:
my_code/catie_calibration/matlab/results/state_tensors_matlab.csv
my_code/catie_calibration/matlab/results/original_reference_all_schedules.csv
my_code/catie_calibration/matlab/results/bma_mixing_reference.csv

# ── Competition analysis source (lives in its own separate repository) ─────────
Data_resources/competition_analysis-main/

# ── Restricted participant data (not for public sharing) ──────────────────────
Data_resources/COMPETITION_data_restricted/

# ── Python virtual environments ───────────────────────────────────────────────
.venv/
__pycache__/
*.pyc

# ── MATLAB auto-save and temp files ───────────────────────────────────────────
*.asv
*.m~
*.mex*

# ── OS artefacts ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.pytest_cache/
